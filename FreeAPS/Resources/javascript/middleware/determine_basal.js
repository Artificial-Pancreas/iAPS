function middleware(iob, currenttemp, glucose, profile, autosens, meal, reservoir, clock, oref2_variables) {
    // modify anything
    // return any reason what has changed.
    return "Nothing changed";
}
